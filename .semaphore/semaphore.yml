version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: s1-more-local-testing
    os_image: ''
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - 'for i in {1..300}; do echo "[$i] this is some output, just for testing purposes" && sleep 1; done'
