the first build that compiles everything and builds the plts takes ~8 minutes

all subsequent builds take ~ one minute

it runs exunit tests (+ one integration tests via wallaby), credo and dialyzer checks
